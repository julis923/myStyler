const ClearModal = ({}) => {

    return (
        <div>
            <p>Are you sure you would like to clear all fields? This cannot be undone.</p>
            <button>yes</button>
            <button>nevermind</button>
        </div>
    )
}

export default ClearModal;